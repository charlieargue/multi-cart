## ASSUMES: $ctx.prev.result   ==========> USER or null

#if($util.isNullOrBlank($ctx.stash.token) || $util.isNull($ctx.prev.result))
    {}
#else
{
    "user": $utils.toJson($ctx.prev.result),
    "token": $utils.toJson($ctx.stash.token)
}
#end