#if($util.isNullOrBlank($context.stash.token) || $util.isNullOrBlank($context.result))
    #set($result = {
    "data": {
            "login": {
                "user": $null,
                "token": $null
            }
        }
    })
    $util.toJson($result)
#else
{
    "user": $utils.toJson($context.result),
    "token": $utils.toJson($context.stash.token)
}
#end