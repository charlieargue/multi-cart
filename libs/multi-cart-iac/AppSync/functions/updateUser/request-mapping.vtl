## ASSUMES: $ctx.prev.result   ==========> USER/ME    $ctx.prev.result.id

{
    "version" : "2017-02-28",
    "operation" : "UpdateItem",
    "key" : {
        "id" : $util.dynamodb.toDynamoDBJson($ctx.prev.result.id)
    },
    "update" : {
        "expression" : "SET currentCartId = :cartId",
        "expressionValues": {
            ":cartId" : $util.dynamodb.toDynamoDBJson($ctx.arguments.currentCartId)
        }
    }
}