mutation AddCartLineAccount(
  $cartId: Int!,
  $cartLineId: Int!,
  $accountNumber: String!,
  $amount: Float!
) {
  addCartLineAccount(
    cartId: $cartId,
    cartLineId: $cartLineId,
    accountNumber: $accountNumber,
    amount: $amount
  ) {
    ...CartLineAccountSnippet
  }
}
