mutation UpdateCartLineAccount(
  $cartId: Int!
  $cartLineId: Int!
  $id: Int!
  $amount: Float!
) {
  updateCartLineAccount(id: $id, amount: $amount) {
    id
    amount
    accountNumber
    cartLineId
  }
}
