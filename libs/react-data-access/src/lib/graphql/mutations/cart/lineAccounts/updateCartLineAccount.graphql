mutation UpdateCartLineAccount(
  $cartId: ID!
  $cartLineId: ID!
  $id: ID!
  $amount: Float!
) {
  updateCartLineAccount(
    cartId: $cartId
    cartLineId: $cartLineId
    id: $id
    amount: $amount
  ) {
    id
    amount
    accountNumber
    cartLineId
  }
}
