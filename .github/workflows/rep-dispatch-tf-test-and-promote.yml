# ###################################################################################################
# ... so at this point, cicd-pipeline has completed, github has merged feature into DEV, ...
# ... triggering Vercel to start the 5 minute countdown towards DEV being ready🔥
# ... 
# ... terraform is planning/applying DEV  (how long does that take?) ????? 🔥 1 min/2 mins?
# ...
# Terraform-triggered Action (after TF DEV Apply completes)
# • will run POSTMAN tests against (newly deployed) DEV ... 4 minutes! 🔥 UNCACHED!
# • will abort on fail


#   ...🔥🔥🔥🔥🔥 it's too close, GOTTA USE preview branch! that started way, way, back
#   ...🔥🔥🔥🔥🔥 upon my local commit!


# • will run CYPRESS E2E tests against wut??? can we assume vercel DEV has been deployed at this stage?
#   -> so 5 minutes after we merge  feature -> DEV, vercel deployment of DEV will be ready
#   •• we can assume the preview has, can't we? or do we need another incoming webhook + action? sigh...
# ✅ SOLUTION: we have to run it against the preview, and so the preview needs a custom domain!
# ###################################################################################################
# thx: https://dev.to/rikurouvila/how-to-trigger-a-github-action-with-an-htt-request-545
# ###################################################################################################

name: tf-test-and-promote
on:
  repository_dispatch:
    types: tf-test-dev-and-promote-to-prod
jobs:
  # ###################################################################################################
  # DEV POSTMAN API/BE TESTING
  # ###################################################################################################
  devPostmanTestingAPI:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]

    steps:
      # NODE VERSION
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      # CHECKOUT feature branch
      - name: Checkout Feature Branch
        uses: actions/checkout@v2

      # cache for each container
      - name: Get Yarn cache directory
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"

      # cache for each container
      - name: Use Yarn cache
        uses: actions/cache@v2
        id: yarn-cache
        with:
          path: |
            **/node_modules
            ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ matrix.node-version }}-${{ hashFiles('**/yarn.lock') }}

      # --------------------------------------
      # TODO: ONLY if code changes to BE/IAC/API code (TODO: when split out into other workflow action files)
      # --------------------------------------

      # INSTALL dependencies
      - name: Install dependencies
        run: yarn install --prefer-offline --frozen-lockfile

      # 🛡 use GITHUB SECRET to create POSTMAN environment file (SECURITY FYI: without sending sensitive keys to LOG, aka the *** replacements)
      - name: Create POSTMAN env file
        run: 'echo "$POSTMAN_ENV" > libs/multi-cart-iac-tests/src/env-variables/Multi-Cart.postman_environment.json'
        shell: bash
        env:
          POSTMAN_ENV: ${{secrets.POSTMAN_SECRETS}}

      # RUN POSTMAN TESTS (against DEV backend)
      - name: 🎾 POSTMAN tests 🎾 (via newman)
        run: yarn test-iac

  # ###################################################################################################
  # develop -> PRODUCTION
  # ###################################################################################################
  developPromotion:
    needs: [devPostmanTestingAPI]
    runs-on: ubuntu-latest
    steps:
      # thx: https://github.com/actions/checkout
      - uses: actions/checkout@v2
        with:
          ref: 'develop'

      - name: Create PROD Pull Request
        uses: peter-evans/create-pull-request@v3
        with:
          # CONFUSING: backwards from githubs "comparing changes" syntax, sigh
          base: develop
          branch: main
